import { colors } from "../../common/globalStyles";

const styles = {
  wrapper: {
    backgroundColor: colors.infoBackground,
    border: "3px solid black",
    padding: "20px",
    display: "flex",
  },
  codeText: { marginBottom: "20px", fontWeight: "bold" },
};

export default styles;
